root = "."
tmp_dir = ".bin"

[build]
  bin = "./.bin/arcane"
  cmd = "CGO_ENABLED=0 go build -o ./.bin/arcane ./cmd"
  full_bin = "./.bin/arcane serve --dev"
  exclude_dir = [".bin", "data"]
  stop_on_error = true

[misc]
  clean_on_exit = true